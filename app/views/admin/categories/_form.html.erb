<div class="form-group">
  <div class="container-fluid">
    <div class="row justify-content-center">
      <div class="col-4">
        <div class="card">
          <div class="card-body">
<%= form_for @category, url: path, method: method_type, local: true do |f| %>

    <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@category.errors.count, "error") %> prohibited
        this category from being saved:
      </h2>
      <ul>
        <% @category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
      <div class="form-label"><%= f.label :name %></div>
      <%= f.text_field :name ,class: 'col-lg-4 form-control'%>
   <br/>
   <div class="form-label"><%= f.label :image %></div>
 <%= f.file_field :image ,class: 'col-lg-4 form-control'%><br /> 

   <div class="btn btn-gray">
      <%= f.submit %>
      </div>
    
    
  <% end %>
   </div>  
</div>
</div>
</div>
</div>
</div>
