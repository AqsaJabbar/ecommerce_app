<div class="form-group">
  <div class="container-fluid">
    <div class="row justify-content-center">
      <div class="col-8">
        <div class="card">
                <div class="card-body">
<p>
<strong>Name:</strong>
  <%= @sub_category.name %>
</p>
<% if @sub_category.image_url %>
      <%= link_to image_tag(@sub_category.image_url())%>
      <%end%>

 <button class="btn btn-light"><%= link_to 'All categories', admin_categories_path%></button>
 <%if can? :edit,@sub_category%>
  <a href="/admin/sub_categories/<%= @sub_category.id %>/edit">Edit </a>
  <%end%>
    </div>
    </div>
    </div>
    </div>
    </div>
    </div>