<div class="form-group">
  <div class="container-fluid">
    <div class="row justify-content-center">
      <div class="col-3">
        <div class="card">
                <div class="card-body">
<%= form_for @order, url: path, method: method_type, local: true do |f| %>
 <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% @order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
     <div class="form-label"><%= f.label :name %></div>
     <%= f.text_field :name,class: 'col-lg-4 form-control' %>
     <br /> 
     <div class="form-label"><%= f.label :phone %></div>
     <%= f.text_field :phone,class: 'col-lg-4 form-control' %>
     <br />
     <div class="form-label"><%= f.label :email %></div>
     <%= f.text_field :email,class: 'col-lg-4 form-control' %>
     <br />
     <div class="form-label"><%= f.label :address %></div>
     <%= f.text_field :address,class: 'col-lg-4 form-control' %>
     <br />
     <%= f.hidden_field :user_id,  :value => current_user.id %>
 
 <div class="btn btn-light" id="order">
    <%= f.submit "Place Order" %>
  </div> 
<% end %>
</div>
</div>
</div>
</div>
</div>
</div>